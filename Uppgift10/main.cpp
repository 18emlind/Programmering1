#include <iostream>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
	using namespace std;
int main() {

int hours, basic, extra, salary1, salary2;

cout <<"Enter the amount of hours you have worked in the last month: " <<endl;

cin >> hours;

basic = 160 * 100;
extra = (hours - 160) * 150;
salary1 = basic + extra;
salary2 = basic;

if (hours > 160 and hours < 241) {
(extra) and (basic); 
cout << salary1; }
else { (basic);
cout << salary2;
} 



	return 0;
}
